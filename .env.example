# Claude API key for the "Frag dein Horoskop" Q&A serverless proxy (api/ask.ts).
# Set this in the Vercel project settings — never commit a real key.
ANTHROPIC_API_KEY=

# Optional: override the model used for the Q&A (defaults to claude-sonnet-4-6).
# ANTHROPIC_MODEL=claude-sonnet-4-6

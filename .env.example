# ── Frontend (Vite) — safe to expose; gated by RLS + edge functions ──
# Vela-Astrology Supabase project (EU). Defaults are baked in src/lib/supabase.ts,
# override here only if you point at a different project.
VITE_SUPABASE_URL=https://khcwkssirzqcwboaisco.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_yjqSH_SLK_VG7XJbGheSxQ_jqAyB9vK

# ── Server-side secrets — set as Supabase Edge Function secrets, NEVER commit ──
# Supabase Dashboard → Project → Settings → Edge Functions → Secrets
#   GEMINI_API_KEY   — Google AI Studio key (aistudio.google.com), starts with AIza…
#   GEMINI_MODEL     — defaults to gemini-3-flash (premium: gemini-3.5-flash)

# ── Legacy: Claude Q&A proxy (api/ask.ts) ──
ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-sonnet-4-6

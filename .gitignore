# OS
.DS_Store

# Dependencies / build
node_modules
dist
dist-ssr
build/
.vite
*.tsbuildinfo
*.log
logs

# Env
.env
.env*.local
*.local
!.env.example

# Deploy
.vercel

# Persönliche Client-Daten — bleiben LOKAL, nie nach GitHub (DSGVO)
Clients/

# Referenz-/Moodboard-Material — lokal
Inspiration/

# Lauras Quellen zu Astrologie & Human Design — Rohmaterial bleibt lokal.
# In die App kommt daraus nur, was als knowledge-Eintrag eingelesen wird.
Wissen/

# claude.ai Artefakt-Export-Cruft
.thumbnail

# Lokale UX-Test-Datei
vela-ux-test.html

# Belege aus dem UX-Lauf — Artefakte, kein Quellcode
screenshots/check/
